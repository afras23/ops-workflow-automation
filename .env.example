# Optional: set to send real Slack webhook. If unset, Slack is mocked to console.
SLACK_WEBHOOK_URL=

# Confidence threshold (0-1). Items below go to human review.
CONFIDENCE_THRESHOLD=0.78

# SQLite path
SQLITE_PATH=data/app.db

# Mock destinations
SHEETS_CSV_PATH=data/sheet_rows.csv
AIRTABLE_JSONL_PATH=data/airtable_rows.jsonl
